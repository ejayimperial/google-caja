- Sandbox rhino properly so that it can't call out to arbitrary java.
- Make object graph walking breadth-first
- Fix back button in AJAX viewer
- JsdocHtmlFormatter to generate searchable HTML
- Update package.html
- Linking to source code.
- Link to javadoc
- Walk over tree after rewriting to find unconsumed doc comments.
- Remove exception handling in resolvePromises and make linkTo safe.

Testcase
(function () {
  /** @see foo */
  function bar() {}

  function foo() {} // not exported

  this.bar = bar;
}();